[project]
name = "cnb-adapters"
version = "0.1.0"
description = "Channel adapter contracts and placeholders"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cnb_adapters"]

