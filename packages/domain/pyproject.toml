[project]
name = "cnb-domain"
version = "0.1.0"
description = "赛博网友机器人的纯领域模型"
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cnb_domain"]
