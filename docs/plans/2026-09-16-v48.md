# 赛博网友机器人开发计划

版本：2026-09-16-v48  
主题：原始 v2 计划状态、中文语境与 MinIO 架构收口  
状态：已完成

## 给 AI 的开发大纲

### 目标

把 `2026-09-09-v2.md` 从早期开发基线收口为与当前仓库事实一致的原始范围记录：P0 至 P7 的代码范围均已完成，MinIO 是开发与生产唯一对象存储，管理后台与运行配置以中文产品语境为准。历史版本仍保留原始语境；本版本只修订当前有效基线和进度索引。

### 实现范围

- 将 v2 的 P0 至 P7 完成状态与 v3、当前代码及测试证据对齐。
- 删除当前有效架构中的 `S3/MinIO` 双实现表述，统一使用 MinIO；协议字段和历史记录保持稳定。
- 明确 GitHub `main` 分支保护受私有仓库套餐限制，作为唯一外部治理阻塞，不影响代码质量门禁。
- 更新总进度和 README，链接本版本并把后续工作切换到产品增强阶段。

### 关键规则

- MinIO 是唯一对象存储实现和产品称谓；不得新增 AWS S3、boto3、botocore 或其他对象存储后端。
- Domain/Cognition 继续保持与 FastAPI、SQLAlchemy、Dramatiq、MinIO SDK 和模型厂商 SDK 解耦。
- 新运行配置必须同步配置定义、校验、管理 API 和后台 UI；Secret 不得明文返回、记录或导出。
- 历史计划不重写；只有 v2 当前有效状态、README 和总进度索引按事实更新。
- 使用中文 Conventional Commit 独立提交并推送到 `origin/main`。

### 验收标准

- v2 的 P0 至 P7 均反映已完成，P0 仅保留分支保护外部限制说明。
- v2 当前有效架构、README、v3 和总进度对 MinIO 决策表述一致。
- README 链接 v48；总进度包含 v48 提交并指向新的下一阶段。
- `uv run poe check`、Web lint/typecheck/test/build、Playwright、OpenAPI 漂移、迁移 head 和 `git diff --check` 通过。

## 验证记录

- `uv run poe check`：通过（Ruff、Pyright、pytest 372 项通过）。
- Web lint、typecheck、Vitest 33 项、build：通过。
- Playwright 22 项、OpenAPI 漂移、迁移 head `20260916_0035`、`git diff --check`：通过。
- 提交：`646e107`（`docs(plans): 收口原始v2计划与MinIO中文语境`）。
