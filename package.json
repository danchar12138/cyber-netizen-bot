{
  "name": "cyber-netizen-bot",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "api:generate": "uv run python -m cnb_api.openapi --output apps/web/openapi.json && corepack pnpm --filter @cnb/web generate:api",
    "web:dev": "corepack pnpm --filter @cnb/web dev",
    "web:check": "corepack pnpm --filter @cnb/web check",
    "web:build": "corepack pnpm --filter @cnb/web build"
  }
}
